# 🧠 MEMORY (단기 기억 저장소)

## 📌 현재 진행 상황
- **[2026-05-04]** **메인 대시보드 리뉴얼 및 리텐션/수익화 시스템 구축**:
    - **UI/UX**: `HomePage`를 개인화 대시보드로 전면 개편 (펫 위젯, 학습 이어하기 버튼 도입).
    - **아키텍처**: `App.jsx` 라우트를 학년별 모듈로 분할하고 `React.lazy`, `ErrorBoundary` 적용으로 성능 및 안정성 확보.
    - **리텐션**: `storageManager.js`에 일일 퀘스트 및 연속 출석(Streak) 시스템 구축 및 `DailyQuestWidget` 구현.
    - **수익화**: 학부모 타겟 광고 배너(`ParentAdBanner`) 설계 및 부모용 대시보드/학습지 페이지 배치.
- **[2026-05-04]** **문장제 생성기 리팩터링 완료**:
    - 3학년 문장제 생성을 템플릿 기반 엔진으로 리팩터링 (하이브리드 아키텍처 도입)
    - 60개 템플릿 구현 (덧셈/뺄셈 17, 곱셈/나눗셈 14, 시간/길이/무게 12, 분수/도형 8, 사고력/규칙/카드 8)
    - 자동 검증 스크립트 구현 (`validate-word-problems.js`) - 59개 템플릿 100% 통과
    - 기존 컴포넌트 호환성 유지 (WordProblemQuiz, WordProblemWorksheet)
- **[2026-05-04]** **SEO 지능형 제출(Change Detection) 도입**:
    - 모든 URL 대신 변경된 URL만 IndexNow에 제출하도록 리팩터링.
    - 빌드 속도 개선을 위한 Batch Mode(일괄 제출) 전환.
    - `node_modules/.cache`를 활용한 빌드 간 상태 추적 구현.
- **[2026-05-04]** **Vercel 빌드 에러 해결**:
    - React 19와 `@react-three/drei` 간의 peer dependency 충돌 해결.
    - `.npmrc`에 `legacy-peer-deps=true` 추가하여 빌드 안정성 확보.
    - `package.json` 의존성 버전을 React 19.0.0으로 정렬.
- **[2026-04-23]** **Phase 2 (고도화 및 게임화) 전 과정 완료**:
    - **2.1 (비주얼)**: AI 펫 튜터 힌트 시스템, 3D 입체도형 실험실(Three.js), 성취도 인증 카드 생성 기능 구현.
    - **2.2 (게임화)**: 마이룸 가구 배치 시스템, XP/레벨 성장 로직, 뱃지 컬렉션 및 상점 카테고리 확장.
    - **2.3 (커뮤니티)**: 익명 랭킹 보드, 소셜 방문 위젯, 학부모 전용 학습 리포트 대시보드(Recharts) 완성.
- **[2026-04-23]** **Bing IndexNow 대응**: Batch Mode 경고 해결을 위해 Streaming Mode(개별 URL 전송)로 전환 완료.
- **[2026-04-23]** **시스템 문서 동기화**: `SYSTEM_MAP`, `CORE_LOGIC`에 신규 로직 및 SEO 변경 사항 반영 완료.

## 🎯 단기 목표 (Next Steps)

### ✅ 완료됨 (2026-05-04)
- [x] **학년별 교과과정 커버리지 감사**: 1~6학년 Curriculum/App.jsx 라우트 매칭 완료
- [x] **Curriculum 데이터 정비**: 오타 수정(smesters→semesters), 단원/도구 추가
- [x] **UI/UX 단순화**: HomePage, GradeSelection, NavigationBar, Curriculum 재구성
- [x] **1,2학년 신규 단원 구현**: 규칙 찾기, 표와 그래프 컴포넌트 4개 및 라우트 완성
- [x] **문장제 생성기 확장**: 3학년 템플릿 기반 리팩터링 완료

### 🚀 향후 과제 (Next Steps)
- [ ] **문장제 생성기 확장**: 4~6학년도 템플릿 기반으로 마이그레이션 검토
- [ ] **성능 최적화**: Three.js 및 Recharts 도입에 따른 초기 로딩 속도 최적화
- [ ] **사용자 피드백 수집**: 실제 초등학생/학부모 대상 사용성 테스트
- [ ] **4학년 컴퍼스 도구**: 4학년 원 단원을 위한 컴퍼스 시뮬레이터 구현
- [ ] **5학년 약수/배수 시각화**: 소인수분해 및 공약수 시각화 도구 고도화

## 📝 최근 결정 사항
- **[2026-05-04]** **문장제 생성기 아키텍처 변경**: 3학년을 템플릿 기반으로 리팩터링하여 유지보수성 개선. 하이브리드 아키텍처(3학년: 템플릿, 그 외: 레거시) 도입으로 안전한 마이그레이션 보장.
- **기술 스택 확장**: 3D 렌더링을 위해 `@react-three/fiber` 및 `three` 도입. 데이터 시각화를 위해 `recharts` 도입.
- **데이터 구조**: `xp`, `level`, `badges`, `roomLayout` 필드를 `storageManager`에 추가하여 게임화 데이터 영속성 확보.
- **라우팅 구조**: 랜딩 페이지 성격의 `HomePage`를 동적 대시보드로 전환하고, 학부모용 `/parent` 경로 신설.

## 🛡️ 가동 원칙 (현행화)
- 3D 컴포넌트 개발 시 `Suspense`를 필수로 사용하여 UX 저하 방지.
- 모든 보상 로직은 `updateCoins` 함수 하나로 통합 관리 (XP 동시 적립).
